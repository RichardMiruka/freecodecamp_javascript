let greeting = "Hello, World!";
// greeting is a variable holding the string "Hello, World!"

let uppercaseGreeting = greeting.toUpperCase();
// .toUpperCase() is a built-in JavaScript string method — it converts all lowercase letters in a string to uppercase, and returns a new string

console.log(uppercaseGreeting);

// The returned value (in this case "HELLO, WORLD!") is assigned to the new variable uppercaseGreeting
