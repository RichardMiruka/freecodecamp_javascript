// checking is something is not equal to another value

console.log(5 != "5");

// the result would be false because the inequality operator first converts 
// the string value to a number and then compares the values
// Since the values would be the same it will return false
