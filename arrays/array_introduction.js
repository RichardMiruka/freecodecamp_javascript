// an ordered collection of values(elements)
// an array is a list of values that are in an ordered format.

const emptyArray = [];
const mixedArray = [1, "ten", {n:3}, [4, 5]];
const nums = [8, 10, 55, 47];


console.log(nums[2]);
nums[1] = 20;
console.log(nums);

// methods used in accessing items in an array

emptyArray.push("Lesson");
console.log(emptyArray);

