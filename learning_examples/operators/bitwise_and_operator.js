/* The bitwise AND (&) operator returns a 1 in each bit position
 * for which the corresponding bits of both operands are 1 
*/
let a = 5; // Binary: 101
let b = 3; // binary: 011
console.log(a & b);
