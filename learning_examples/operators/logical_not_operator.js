/* represented by an exclamation mark (!) 
 *  It flips the boolean value of its operand
 *  if the operand is true, it becomes false, and if it's false, it becomes true
*/
let isOnline = true;
console.log(!isOnline);

let isOffline = false;
console.log(!isOffline);
