// lesser than operator, represented by a left-angle bracket (<)
//  checks if the value on the left is smaller than or equal to the one on the right

let a = 6;
let b = 9;
let c = 6;

console.log(a <= b);
console.log(b <= a);
console.log(c <= a);
