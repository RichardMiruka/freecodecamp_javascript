// The right shift (>>) operator shifts all bits to the right.

let a = 5;
console.log(a >> 1); // Output: 2 (Binary: 10)

// Here, all bits are shifted one position to the right,
// effectively dividing the number by 2 and rounding down
