/* greater than or equal comparison operator ( >= )
 * checks if the value on the left is either greater than
 * or equal to the one on the right
 */

let a = 6;
let b = 9;
let c = 6;

console.log(a >= b);
console.log(b >= a);
console.log(a >= c);
